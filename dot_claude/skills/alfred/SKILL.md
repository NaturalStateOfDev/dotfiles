---
name: alfred
description: Alias for /staff. Use when the user types /alfred <anything>; behaves exactly like /staff with the same arguments (task or tickets, review, status, cleanup <PR>).
---

# /alfred — alias for /staff

Invoke the `staff` skill via the Skill tool with `args` set to the arguments below, verbatim. Do nothing else in this skill; the `staff` skill owns the routing table.

ARGUMENTS: $ARGUMENTS
