---
name: staff
description: Alias for /alfred. Use when the user types /staff <anything>; behaves exactly like /alfred with the same arguments (task or tickets, review, status, cleanup <PR>).
---

# /staff — alias for /alfred

Invoke the `alfred` skill via the Skill tool with `args` set to the arguments below, verbatim. Do nothing else in this skill; the `alfred` skill owns the routing table.

ARGUMENTS: $ARGUMENTS
